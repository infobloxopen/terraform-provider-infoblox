# Auto-generated resource acceptance-test cases for Address (uddi).
# Source: terraform-provider-uddi. One labelled `case` block per legacy
# TestAccAddressResource_<Case>.
# Replayed by the infoblox provider via acctest.RunResourceCases.

case "basic" {
  # basic — generated from terraform-provider-uddi
  backend = "uddi"
  skip        = true
  skip_reason = "unsupported config (prerequisites or nested values)"
}

case "disappears" {
  # disappears — generated from terraform-provider-uddi
  backend = "uddi"
  skip        = true
  skip_reason = "unsupported config (prerequisites or nested values)"
}

case "address" {
  # address — generated from terraform-provider-uddi
  backend = "uddi"
  skip        = true
  skip_reason = "unsupported config (prerequisites or nested values)"
}

case "comment" {
  # comment — generated from terraform-provider-uddi
  backend = "uddi"
  skip        = true
  skip_reason = "unsupported config (prerequisites or nested values)"
}

case "hwaddr" {
  # hwaddr — generated from terraform-provider-uddi
  backend = "uddi"
  skip        = true
  skip_reason = "unsupported config (prerequisites or nested values)"
}

case "interface" {
  # interface — generated from terraform-provider-uddi
  backend = "uddi"
  skip        = true
  skip_reason = "unsupported config (prerequisites or nested values)"
}

case "names" {
  # names — generated from terraform-provider-uddi
  backend = "uddi"
  skip        = true
  skip_reason = "unsupported config (prerequisites or nested values)"
}

case "space" {
  # space — generated from terraform-provider-uddi
  backend = "uddi"
  skip        = true
  skip_reason = "unsupported config (prerequisites or nested values)"
}

case "tags" {
  # tags — generated from terraform-provider-uddi
  backend = "uddi"
  skip        = true
  skip_reason = "unsupported config (prerequisites or nested values)"
}

case "next_available_id_count" {
  # next_available_id_count — generated from terraform-provider-uddi
  backend = "uddi"
  skip        = true
  skip_reason = "unsupported config (prerequisites or nested values)"
}

case "next_available_subnet" {
  # next_available_subnet — generated from terraform-provider-uddi
  backend = "uddi"
  skip        = true
  skip_reason = "unsupported config (prerequisites or nested values)"
}

case "next_available_address_block" {
  # next_available_address_block — generated from terraform-provider-uddi
  backend = "uddi"
  skip        = true
  skip_reason = "unsupported config (prerequisites or nested values)"
}

case "next_available_range" {
  # next_available_range — generated from terraform-provider-uddi
  backend = "uddi"
  skip        = true
  skip_reason = "unsupported config (prerequisites or nested values)"
}
