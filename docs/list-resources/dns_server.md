---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "infoblox_dns_server List Resource - terraform-provider-infoblox"
subcategory: "DNS"
description: |-
  Retrieves a list of Infoblox DnsServer from the UDDI backend.
---

# infoblox_dns_server (List Resource)

Retrieves a list of Infoblox DnsServer from the UDDI backend.

## Example Usage

### UDDI Backend

```terraform
// List specific Dns Servers using filters
list "infoblox_dns_server" "list_dns_server_using_filters" {
  provider = infoblox
  config {
    filters = {
      name = "example_server"
    }
  }
  limit = 10
}

// List specific Dns Servers using Tags
list "infoblox_dns_server" "list_dns_server_using_tags" {
  provider = infoblox
  config {
    tag_filters = {
      Site = "location-1"
    }
  }
}

// List Dns Servers with resource details included
list "infoblox_dns_server" "list_dns_server_with_resource" {
  provider         = infoblox
  include_resource = true
}
```


<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `filters` (Map of String) Filters are used to return a more specific list of results. Filters can be used to match resources by specific attributes (e.g. name, view). If multiple filters are specified, only resources that match all of them are returned.
- `tag_filters` (Map of String) Tag Filters are used to filter results by UDDI tags. Only applicable for the UDDI backend.
