---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "infoblox_ipv6_sharednetwork List Resource - terraform-provider-infoblox"
subcategory: "DHCP"
description: |-
  Retrieves a list of Infoblox Ipv6sharednetwork from the NIOS backend.
---

# infoblox_ipv6_sharednetwork (List Resource)

Retrieves a list of Infoblox Ipv6sharednetwork from the NIOS backend.

## Example Usage

### NIOS Backend

```terraform
// List specific IPv6 Shared Networks using filters
list "infoblox_ipv6_sharednetwork" "list_ipv6_sharednetwork_by_filters" {
  provider = infoblox
  config {
    filters = {
      name = "example_ipv6_shared_network1"
    }
  }
  limit = 10
}

// List specific IPv6 Shared Networks using Extensible Attributes
list "infoblox_ipv6_sharednetwork" "list_ipv6_sharednetwork_by_ext_attrs" {
  provider = infoblox
  config {
    ext_attr_filters = {
      Site = "location-1"
    }
  }
}

// List IPv6 Shared Networks with resource details included
list "infoblox_ipv6_sharednetwork" "list_ipv6_sharednetwork_with_resource" {
  provider         = infoblox
  include_resource = true
}
```


<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `ext_attr_filters` (Map of String) Extensible Attribute Filters are used to filter results by NIOS extensible attributes. Only applicable for the NIOS backend.
- `filters` (Map of String) Filters are used to return a more specific list of results. Filters can be used to match resources by specific attributes (e.g. name, view). If multiple filters are specified, only resources that match all of them are returned.
