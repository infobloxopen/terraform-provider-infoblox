---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "infoblox_federated_realm List Resource - terraform-provider-infoblox"
subcategory: "IPAM FEDERATION"
description: |-
  Retrieves a list of Infoblox FederatedRealm from the UDDI backend.
---

# infoblox_federated_realm (List Resource)

Retrieves a list of Infoblox FederatedRealm from the UDDI backend.

## Example Usage

### UDDI Backend

```terraform
// List specific Federated Realms using filters
list "infoblox_federated_realm" "list_federated_realm_using_filters" {
  provider = infoblox
  config {
    filters = {
      name = "example_federated_realm"
    }
  }
  limit = 10
}

// List specific Federated Realms using tags
list "infoblox_federated_realm" "list_federated_realm_using_tags" {
  provider = infoblox
  config {
    tag_filters = {
      site = "Site A"
    }
  }
}

// List Federated Realms with resource details included
list "infoblox_federated_realm" "list_federated_realm_with_resource" {
  provider         = infoblox
  include_resource = true
}
```


<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `filters` (Map of String) Filters are used to return a more specific list of results. Filters can be used to match resources by specific attributes (e.g. name, view). If multiple filters are specified, only resources that match all of them are returned.
- `tag_filters` (Map of String) Tag Filters are used to filter results by UDDI tags. Only applicable for the UDDI backend.
