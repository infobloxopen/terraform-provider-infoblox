---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "infoblox_superhost Resource - terraform-provider-infoblox"
subcategory: "IPAM"
description: |-
  Manages an Infoblox Superhost in the NIOS backend.
---

# infoblox_superhost (Resource)

Manages an Infoblox Superhost in the NIOS backend.

## Example Usage

### NIOS Backend

```terraform
// Create an Auth Zone (Required as Parent)
resource "infoblox_zone_auth" "parent_zone" {
  nios = {
    fqdn = "example_super_host_zone.com"
    view = "default"
  }
}

// Create a Record A (Required as Parent)
resource "infoblox_record_a" "parent_record_a" {
  nios = {
    name     = "parent_record.${infoblox_zone_auth.parent_zone.nios.fqdn}"
    ipv4addr = "10.20.1.2"
    view     = "default"
  }
}

// Create Super Host with Basic Fields
resource "infoblox_superhost" "create_super_host" {
  nios = {
    name = "example_super_host"
  }
}

// Create Super Host with Additional Fields
resource "infoblox_superhost" "create_super_host_with_additional_fields" {
  nios = {
    name = "example_super_host_with_associated_objects"

    // Additional Fields
    comment                   = "This is a Super Host example"
    delete_associated_objects = true
    disabled                  = false
    dns_associated_objects    = [infoblox_record_a.parent_record_a.id]

    // Extensible Attributes
    ext_attrs = {
      Site = "location-1"
    }
  }
}
```


<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `nios` (Attributes) NIOS backend-specific fields. (see [below for nested schema](#nestedatt--nios))

### Read-Only

- `id` (String) The reference to the object.

<a id="nestedatt--nios"></a>
### Nested Schema for `nios`

Required:

- `name` (String) Name of the Superhost.

Optional:

- `comment` (String) The comment for Super Host.
- `delete_associated_objects` (Boolean) True if we have to delete all DNS/DHCP associated objects with Super Host, false by default.
- `dhcp_associated_objects` (List of String) A list of DHCP objects refs which are associated with Super Host.
- `disabled` (Boolean) Disable all DNS/DHCP associated objects with Super Host if True, False by default.
- `dns_associated_objects` (List of String) A list of object refs of the DNS resource records which are associated with Super Host.
- `ext_attrs` (Map of String) Extensible attributes associated with the object. For valid values for extensible attributes, see {extattrs:values}.

Read-Only:

- `ext_attrs_all` (Map of String) All ext_attrs including Terraform Internal ID and inherited attributes.
