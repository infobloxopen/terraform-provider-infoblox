---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "infoblox_nsgroup_delegation Resource - terraform-provider-infoblox"
subcategory: "DNS"
description: |-
  Manages an Infoblox NsgroupDelegation in the NIOS backend.
---

# infoblox_nsgroup_delegation (Resource)

Manages an Infoblox NsgroupDelegation in the NIOS backend.

## Example Usage

### NIOS Backend

```terraform
// Manage an NS group delegation
resource "infoblox_nsgroup_delegation" "nsgroup_delegation_basic_fields" {
  nios = {
    name = "example_ns_group_delegation_basic"
    delegate_to = [
      {
        address = "2.3.3.4"
        name    = "delegate_to_ns_group"
      }
    ]
  }
}

// Manage an NS group delegation with additional attributes
resource "infoblox_nsgroup_delegation" "nsgroup_delegation_with_additional_fields" {
  nios = {
    name = "example_ns_group_delegation"
    delegate_to = [
      {
        address = "2.3.3.5"
        name    = "delegate_to_ns_group"
      }
    ]
    comment = "Create NS Group Delegation"
    ext_attrs = {
      Site = "location-1"
    }
  }
}
```


<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `nios` (Attributes) NIOS backend-specific fields. (see [below for nested schema](#nestedatt--nios))

### Read-Only

- `id` (String) The reference to the object.

<a id="nestedatt--nios"></a>
### Nested Schema for `nios`

Required:

- `delegate_to` (Attributes List) The list of delegated servers for the delegated NS group. (see [below for nested schema](#nestedatt--nios--delegate_to))
- `name` (String) The name of the delegated NS group.

Optional:

- `comment` (String) The comment for the delegated NS group.
- `ext_attrs` (Map of String) Extensible attributes associated with the object. For valid values for extensible attributes, see {extattrs:values}.

Read-Only:

- `ext_attrs_all` (Map of String) All ext_attrs including Terraform Internal ID and inherited attributes.

<a id="nestedatt--nios--delegate_to"></a>
### Nested Schema for `nios.delegate_to`

Required:

- `address` (String) The IPv4 Address or IPv6 Address of the server.
- `name` (String) A resolvable domain name for the external DNS server.

Optional:

- `stealth` (Boolean) Set this flag to hide the NS record for the primary name server from DNS queries.
- `tsig_key` (String) A generated TSIG key.
- `tsig_key_alg` (String) The TSIG key algorithm.
- `tsig_key_name` (String) The TSIG key name.
- `use_tsig_key_name` (Boolean) Use flag for: tsig_key_name
